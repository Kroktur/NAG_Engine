
project(Math)

set(HEADER_DIR ${CMAKE_CURRENT_SOURCE_DIR}/include)
set(SOURCE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/src)


set(HEADERS
 ${HEADER_DIR}/Vector.h
 ${HEADER_DIR}/Iterator.h
 ${HEADER_DIR}/RevIterator.h
 ${HEADER_DIR}/Utility.h
 ${HEADER_DIR}/Array.h
 ${HEADER_DIR}/VectorND.h
 ${HEADER_DIR}/Vec2.h
 ${HEADER_DIR}/Vec3.h
 ${HEADER_DIR}/Matrice.h
 ${HEADER_DIR}/TestMath.h
 ${HEADER_DIR}/Angle.h
 ${HEADER_DIR}/Pair.h
)
set(SOURCES
${SOURCE_DIR}/TestMath.cpp
${SOURCE_DIR}/Angle.cpp
)



add_library(${PROJECT_NAME}
${SOURCES}
${HEADERS}
)

add_executable(MathTest
    ${HEADERS}
    ${SOURCES}
)
target_include_directories(${PROJECT_NAME} PUBLIC ${HEADER_DIR})
target_link_libraries(${PROJECT_NAME}
    PUBLIC
    TestUnits
)
target_link_libraries(MathTest
    PUBLIC
    TestUnits
)
set_target_properties(${PROJECT_NAME} PROPERTIES FOLDER "Math")